/*const precos = document.getElementById("precos");
const adicionar = document.getElementById("adiciona");


precos.addEventListener('click', exibirTabela);

function exibirTabela() {
    console.log('click');
    window.location.href = "precos.html";
}


//adicionar.addEventListener('click', registrar);

function registrar() {
    const infos = {
        placa: document.getElementById('placa').value,
        nome: document.getElementById('nome').value
    };
    const infos = [];

    info = { placa, nome }

    infos.push(info)
    localStorage.setItem("infos", JSON.stringify(infos));

    // var storedNames = JSON.parse(localStorage.getItem("infos"));


    console.log("alo", info);
}*/